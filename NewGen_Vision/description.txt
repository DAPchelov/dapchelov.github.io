Список замечаний к тестовому скрипту:

1. убрать объявление полей username и password вне конструктора;
2. геттер username должен возвращать this._username;
3. геттер password не имеет смысла т.к. всегда возвращает только ошибку, необходимо сделать так чтобы он возвращал this._password;
4. нужно добавить сеттеры username и password
5. метод authenticate_user не должен быть статическим т.к. он вызывается для конкретного объекта;
6. при вызове метода open объекта xhr и составлении запроса нужно обратиться к свойствам объекта UserService с помощью ключевого слова this вместо имени класса;
7. функцию присвоения значения переменной result, в методе authenticate_user, упразднить, а return заменить на следующее выражение: return (xhr.onload = (xhr.status === '200') ? true : xhr.response);
8. обработчик JQuery обернуть в addEventListener с параметром DOMContentLoaded чтобы событие click не происходило раньше времени;
9. упразднить переменные username и password, а значения получать из формы при объявлении res;
10. условный оператор if заменить на тернарный оператор: (res === true) ? document.location.href = '/home' : alert(res.error);