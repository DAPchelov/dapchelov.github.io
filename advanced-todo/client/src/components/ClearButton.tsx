import Button from '@mui/material/Button';

const ClearButton: React.FC = () => {
    return (
        <Button variant="text" sx={{ fontSize: 10 }}>clear completed</Button>
    );
}

export default ClearButton;