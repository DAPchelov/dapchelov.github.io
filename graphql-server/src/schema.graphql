type Message {
  id: String!
  name: String!
  text: String!
}

type Query {
  message(id: String!): Message!
  messages: [Message!]!
}

type Mutation {
  createMessage(id: String!, name: String!, text: String!): Message!
  deleteMessage(id: String!): Message!
}
