type Message {
  id: String!
  user: String!
  content: String!
}

type Query {
  message(id: String!): Message!
  messages: [Message!]!
}

type Mutation {
  postMessage(user: String!, content: String!): ID!
  deleteMessage(id: String!): Message!
}
 type Subscription {
     messages: [Message!]!
 }